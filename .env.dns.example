# Hurricane Electric example. Create secrets/hurricane_tokens first.
HURRICANE_TOKENS_FILE=/run/secrets/hurricane_tokens

# Replace these variables with the selected provider's documented lego
# environment variables. Use *_FILE only when that provider supports it.
